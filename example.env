# LLM Configuration
# Set LLM_PROVIDER to 'gemini' or 'ollama' to choose your LLM service.
LLM_PROVIDER=gemini

# Gemini Configuration (if LLM_PROVIDER is 'gemini')
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
GEMINI_MODEL=gemini-2.5-flash

# Ollama Configuration (if LLM_PROVIDER is 'ollama')
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama2

# Cache Directory
# Specifies the directory for caching downloaded content. Defaults to 'data/cache'.
CACHE_DIR=data/cache

# Request Timeout
# Specifies the timeout for network requests in seconds. Defaults to 30 seconds.
REQUEST_TIMEOUT=30